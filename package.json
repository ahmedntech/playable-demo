{
  "name": "playable-platform",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "No-code playable ad builder — thin-slice MVP (editor + runtime + single-HTML export)",
  "scripts": {
    "build:runtime": "vite build --config vite.runtime.config.ts",
    "dev": "npm run build:runtime && vite",
    "build": "npm run build:runtime && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "pixi.js": "^8.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
